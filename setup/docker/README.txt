/***** PLEASE READ THIS BEFORE STARTING THE DOCKER ENVIRONMENT ******/

Please override the docker-compose.yml file with the appropriate docker-compose.yml from the environments folder.

For example, for the local environment please override the root docker-compose.yml with the version available in: 
	environments/local/docker-compose.yml